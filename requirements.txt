# Core (tested with Python 3.12, torch 2.11 on macOS CPU; any torch >= 2.0 with CUDA works on the cluster)
torch>=2.0
numpy>=1.24
gymnasium==0.29.1
pettingzoo==1.24.3
supersuit==3.9.3
tyro==0.9.2
tensorboard==2.18.0

# Melting Pot substrates (Linux only: dm-meltingpot depends on dmlab2d).
# `pip install "shimmy[meltingpot]==1.3.0"` pulls dm-meltingpot>=2.2.0.
shimmy[meltingpot]==1.3.0

# Optional
pytest==8.3.4        # tests
imageio==2.36.1      # evaluate.py --video
# wandb              # --track
